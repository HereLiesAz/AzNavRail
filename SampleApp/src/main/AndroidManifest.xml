<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SampleApp">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SampleApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The High-Inference API demo: its rail comes entirely from `@Az` annotations, via the
             graph the KSP processor generates. Not the launcher; start it explicitly. -->
        <activity
            android:name=".AzGraphDemoActivity"
            android:exported="false"
            android:label="AzGraph Demo"
            android:theme="@style/Theme.SampleApp" />

        <service
            android:name=".SampleOverlayService"
            android:exported="false" />
    </application>

</manifest>
