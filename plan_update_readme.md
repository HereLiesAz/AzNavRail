1. We need to go through the `README.md` and `docs/AZNAVRAIL_COMPLETE_GUIDE.md` files.
2. For every explanation or instruction for using the Android library, there should be an equivalent explanation or instruction for react (`aznavrail-react`).
3. If Android explains how to use the DSL `AzNavRail(...) { ... }`, there should be an equivalent React documentation block using JSX: `<AzNavRail ... > <AzNavItem ... /> </AzNavRail>`.
4. The instructions also apply to `docs/AZNAVRAIL_COMPLETE_GUIDE.md`.

Wait, the user wants me to do this now: "Pull the latest snapshot of the default branch. Then, go through the readme and complete guide. For every explanation or instruction for using the Android library, there should be an equivalent explanation or instruction for react."

I should make these changes and then submit.
