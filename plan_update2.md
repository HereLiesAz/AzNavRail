The user has added: "We also need a github action to publish this library to Github Packages for installation using npm".
So I need to:
1. Create a `.github/workflows/publish-npm.yml` action workflow that publishes `aznavrail-react` to GitHub Packages whenever a release is created or pushed to main with a version tag.
I will set the plan to include creating this GitHub Action.

Let's modify the plan.
